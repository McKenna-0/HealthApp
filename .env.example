# Copy to .env and fill in. .env is gitignored.

# "mock" (default, no Garmin needed) or "garmin"
DATA_SOURCE=mock

# IANA timezone used for "today" everywhere
TZ=Europe/Dublin

# Garmin Connect credentials (only needed when DATA_SOURCE=garmin).
# Password is used once to obtain OAuth tokens, which are cached in
# backend/.garmin_tokens/ and auto-refreshed after that.
GARMIN_EMAIL=
GARMIN_PASSWORD=

# Free API key from https://fdc.nal.usda.gov/api-key-signup.html
# (optional - Open Food Facts works without any key)
USDA_API_KEY=

# AI analyst (optional). Any OpenAI-compatible endpoint works.
# Default: OpenRouter (https://openrouter.ai/keys). Prefer paid open models
# with no-logging policies over ":free" variants for privacy.
AI_BASE_URL=https://openrouter.ai/api/v1
AI_API_KEY=
AI_MODEL=meta-llama/llama-3.3-70b-instruct

# Agentic chat. Its own provider triple, so chat can run on a stronger or more
# private endpoint than the weekly report. Blank base URL / key fall back to the
# AI_* values above. All three can also be set from the app (Settings > AI),
# which takes precedence over anything here.
# The model must support tool calling - check `supported_parameters` at
# https://openrouter.ai/api/v1/models before switching.
AI_AGENT_BASE_URL=
AI_AGENT_API_KEY=
AI_AGENT_MODEL=deepseek/deepseek-v4-flash

# Ask OpenRouter to route only to endpoints that don't retain or train on
# prompts. Open weights alone do NOT mean private - whoever hosts the model
# still sees every request - so leave this on unless you self-host.
# Ignored by non-OpenRouter endpoints.
AI_REQUIRE_ZDR=true

# GitHub issue poller (optional — for auto-implementing issues via Claude CLI)
# GITHUB_REPO=McKenna-0/HealthApp
# POLL_INTERVAL_MINUTES=5
